using System;

class Program
{
    static void Main()
    {
        Console.Write("N daxil edin: ");
        int N = int.Parse(Console.ReadLine());

        long a = 1, b = 1;

        Console.Write("Fibonacci ardıcıllığı: ");

        for (int i = 1; i <= N; i++)
        {
            Console.Write(a + " ");
            long temp = a + b;
            a = b;
            b = temp;
        }

        Console.WriteLine();
    }
}
